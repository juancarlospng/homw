{
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build && npm run build:explorerapp",
    "build:explorerapp": "npm --prefix explorerapp ci && npm --prefix explorerapp run build && node scripts/copy-explorerapp.cjs",
    "preview": "vite preview --host 127.0.0.1",
    "serve": "node scripts/serve-dist.cjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "lucide-react": "^1.18.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^5.4.0"
  }
}
